/*usando una variable contador y bucle que recorra el array, determinael tamaño del siguiente array*/
let array = [7, 4, 0, 9, 7, 6, 3, 8;
let size = 0;

for (let i = 0; i < array.length; i++) {
    size++;
}

console.log("El tamaño del array es: " + size);